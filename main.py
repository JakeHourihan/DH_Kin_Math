"""This mathematical model uses modified DH parameters that reference the 
previous joint axis i-1 as opossed to traditional models that reference i.
This affects how the table is filled and incorrectly doing so will negatively impact 
the kinematic calculations"""

import math
import numpy as numpy

dhTable = np.array[]



numJoints = input('How many joints does your robot have? ')
numJoints = int(numJoints)

for joint in numJoints:
    ai1
    theta
    d1
    